# Modbus_communication_spring

1. build mvn
mvn clean compile
mvn package || mvn compile assembly:single

2. build docker
docker build -t iot/sentron_spring .

3. run docker-compose from docker-compose.yml

